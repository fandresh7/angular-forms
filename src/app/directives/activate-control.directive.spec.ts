import { ActivateControlDirective } from './activate-control.directive'

describe('ActivateControlDirective', () => {
  it('should create an instance', () => {
    const directive = new ActivateControlDirective()
    expect(directive).toBeTruthy()
  })
})
